# 신청폼 검수 결과

최초 검수일: 2026-08-25  
재검수일: 2026-08-28, 2026-09-04

## 자동 검사

| 항목 | 결과 | 비고 |
| --- | --- | --- |
| React·TypeScript 빌드 | 통과 | `npm run build` |
| Vercel API TypeScript 검사 | 통과 | 빌드 명령에 `tsconfig.api.json` 포함 |
| ESLint | 통과 | `npm run lint` |
| 런타임 계약 회귀 테스트 | 7개 통과 | `npm test`; v3 스키마·문자열 시간·순위·신청 필드·9단계 퍼널·재시작 ID 검증 |
| 이미지 참조 검색 | 통과 | 화면 코드에 래스터 이미지와 이미지 태그 없음 |
| HTML 언어와 기본 메타데이터 | 통과 | `lang="ko"`, 설명, Open Graph 텍스트 |

## 구현 확인

- [x] 하단 코치 이미지가 렌더링되지 않습니다.
- [x] 4개 질문을 이전·다음으로 이동할 수 있습니다.
- [x] 필수 선택 전 다음 단계 이동을 막고 한국어 오류를 표시합니다.
- [x] 편안한 순간과 버거운 순간은 기본값 없이 사용자가 1~100자의 텍스트로 입력합니다.
- [x] 질문 화면에는 선택 답변 노트 미리보기가 렌더링되지 않습니다.
- [x] 4단계 완료 뒤 LIFE SESSION 안내를 거쳐 신청 정보 입력으로 이동합니다.
- [x] 화면에 원형 하루 지도와 관련 문구가 표시되지 않습니다.
- [x] 인트로, 질문, 연락 신청과 완료 화면이 노트형 시각 언어를 유지합니다.
- [x] 변화 영역을 누른 순서대로 1·2·3순위로 표시하고 저장합니다.
- [x] 60분 프로그램의 다섯 구간과 일주일 뒤 후속 만남을 안내합니다.
- [x] 첫 화면에서 3분 기록 후 오프라인 Life Session을 신청한다는 목적을 확인할 수 있습니다.
- [x] 1280×720 화면에서 시작 CTA가 초기 화면 안에 표시됩니다.
- [x] 390×844 브라우저 뷰포트에서 가로 잘림 없이 자연스럽게 세로 스크롤할 수 있습니다.
- [x] 320×568 브라우저 뷰포트에서 가로 잘림 없이 질문과 CTA에 접근할 수 있습니다.
- [x] 데스크톱 질문 화면에서 로고, 진행선, 제목, 카드와 버튼이 같은 본문 기준선에 맞습니다.
- [x] 모바일 질문 화면에서 로고와 질문 본문의 왼쪽 기준선이 일치합니다.
- [x] 700px 이하 연락 화면에서도 필수 입력과 제출 CTA에 세로 스크롤로 접근할 수 있습니다.
- [x] 이름, 나이, 전화번호, 주변 역과 인터뷰 가능 요일·시간대를 필수로 검증합니다.
- [x] 전화번호 입력을 `010-0000-0000` 형식으로 자동 정리합니다.
- [x] 필수 연락 입력 오류 시 해당 입력으로 포커스가 이동합니다.
- [x] 퍼널은 4단계 다음 `session_info_viewed`, `application_started`로 이어집니다.
- [x] 신청 스키마는 `daylog-life-session-v3`이며 에너지 순간을 1~100자 문자열로 처리합니다.
- [x] 제출 실패 시 입력 상태를 유지합니다.
- [x] 개인정보는 브라우저 저장소에 넣지 않습니다.
- [x] 퍼널 요청에는 답변과 연락처가 포함되지 않습니다.
- [x] Vercel API가 외부 입력을 실행 시에 재검증합니다.

## 2026-09-04 재검수

| 항목 | 결과 | 비고 |
| --- | --- | --- |
| React·TypeScript 빌드 | 통과 | `npm run build` |
| ESLint | 통과 | `npm run lint` |
| 런타임 계약·API 회귀 테스트 | 20개 통과 | `npm test`; v3 스키마·문자열 시간·기타 입력 payload 제외·순위·신청 필드·9단계 퍼널·재시작 ID·API 서버 검증 |

- [x] 힘이 나는/빠지는 순간이 시간대가 아닌 생활 상황 버튼 7개 + 기타로 표시됩니다.
- [x] `기타` 선택 시 화면별 라벨과 예시가 있는 입력란이 열리고 자동 포커스됩니다.
- [x] `기타` 입력 없이 다음을 누르면 입력란에 오류와 포커스가 표시됩니다.
- [x] 다른 버튼으로 바꿨다가 `기타`로 돌아오면 작성한 값이 보존됩니다.
- [x] 새로고침 후에도 `기타` 선택과 입력값이 복원됩니다.
- [x] `기타` 입력은 제출 시 `기타: <내용>` 문자열로 기존 필드에 담기고, 초안 상태는 제출 payload에서 제외됩니다.
- [x] 인트로부터 완료까지 모든 화면의 가운데 패널이 560px로 통일되고, 신청 화면의 720px 예외가 사라졌습니다.
- [x] 콘텐츠가 짧은 화면에서도 공통 최소 높이 안에서 하단 버튼이 패널 아래쪽에 고정됩니다.
- [x] 320×568, 375×812, 1280×720에서 가로 잘림이 없습니다.
- [ ] 200% 확대, 실제 iOS·Android 키보드와 VoiceOver·NVDA는 브라우저 자동화로 확인하지 못했습니다.

## 2026-09-04 재검수 (전 챕터 중앙 패널 동일화)

| 항목 | 결과 | 비고 |
| --- | --- | --- |
| React·TypeScript 빌드 | 통과 | `npm run build` |
| ESLint | 통과 | `npm run lint` |
| 런타임 계약·API 회귀 테스트 | 20개 통과 | `npm test` |
| 챕터별 패널 크기 실측 | 통과 | `getBoundingClientRect()`로 1280×720(560×560px)·375×812(335×640px) 8개 챕터 동일 확인 |

- [x] 같은 viewport에서 인트로~신청 1/3까지 패널 너비·높이가 완전히 동일합니다(1px 이하 오차).
- [x] 습관 선택(9개 카드) 화면에서 패널 크기 변화 없이 내부 스크롤로 모든 카드에 접근할 수 있습니다.
- [x] 활력·피로 `기타` 입력을 펼쳐도 패널이 커지지 않고 본문 스크롤 범위만 늘어납니다.
- [x] 이전·다음·제출 버튼이 본문 길이와 관계없이 패널 하단에 고정되어 보입니다.
- [x] 챕터 전환 시 내부 스크롤이 맨 위에서 시작합니다.
- [x] 바깥 문서와 패널 내부의 이중 스크롤이 발생하지 않습니다(`.app-panel { overflow: hidden }`).
- [ ] 390×844, 820×1180, 1440×900 실측, 200% 확대와 실제 모바일 키보드 상태는 확인하지 못했습니다.

## 운영 환경에서 남은 검수

- [ ] 실제 Google Apps Script 코드 추가와 새 버전 배포
- [ ] Vercel Production·Preview 환경변수 등록
- [ ] 실제 신청 성공, 중복 신청과 네트워크 오류 확인
- [ ] 완료 후 다시 시작할 때 새 `sessionId`와 `requestId`가 생성되는지 확인
- [ ] 세 Google Sheets 탭의 헤더, 표시값과 퍼널 수식 확인
- [ ] 기존 다섯 폼 회귀 테스트
- [ ] 실제 iPhone Safari, Android Chrome과 태블릿 실기기 확인
- [ ] 공개 개인정보 처리방침 링크와 보관 기간 반영

운영 비밀값과 실제 Apps Script URL이 제공되지 않은 상태이므로 실제 시트 기록 검수는 아직 수행하지 않았습니다.
